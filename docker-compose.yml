version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb+srv://EchoDB:KoGYETSiHQYJTVj0@cluster0.sh9hk.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=yogeshmahajan443
      - NODE_ENV=PRODUCTION
      - CLIENT_URL=http://localhost:5173
      - ADMIN_SECRET_KEY=adsasdsdfsdfsdfd
      - CLOUDINARY_CLOUD_NAME=do5sjxvjl
      - CLOUDINARY_API_KEY=886359441637273
      - CLOUDINARY_API_SECRET=G-MUDg_iQwUCzUHZGpCCRqIqJPM
    depends_on:
      - mongo

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
    - VITE_SERVER=http://localhost:3000
    depends_on:
      - backend

  mongo:
    image: mongo:5.0
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
